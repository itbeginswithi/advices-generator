import React from "react";

class App extends React.Component {
  state = {
    advice: "",
  };
  
  fetchAdvice = async () => { 
     try {
         const response = 
     } catch (error) {
         console.error(error);
     }
  }

  render() {
    return <div>App</div>;
  }
}

export default App;
